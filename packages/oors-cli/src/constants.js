import path from 'path';

export const SRC_DIR = 'src';
export const BUILD_DIR = 'build';
export const PACKAGES_DIR = path.resolve(__dirname, '../packages');
export const IGNORED_PACKAGES = ['web-app'];
